apiVersion: v1
kind: Service
metadata:
  name: {{ include "payer_extractor.fullname" . }}
  labels:
    {{- include "payer_extractor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "payer_extractor.selectorLabels" . | nindent 4 }}
