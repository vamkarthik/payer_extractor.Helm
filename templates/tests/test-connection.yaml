apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "payer_extractor.fullname" . }}-test-connection"
  labels:
    {{- include "payer_extractor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "payer_extractor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
